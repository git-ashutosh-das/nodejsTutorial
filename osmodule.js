import os from 'os'; // os mudule is an built in module, so ./ is not rquired in the path - Useful module explore some functions
// gets info about os in the machine - your pc
console.log(os.freemem())
console.log(os.homedir())
console.log(os.hostname())
console.log(os.type())


